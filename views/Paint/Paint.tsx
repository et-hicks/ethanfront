import React from 'react'

import WebPaint from "@components/Painting/WebPaint"


export default function PaintView() {
    
    return (
        <div>
            Paint View is now done!
            <WebPaint />
        </div>
    );
}